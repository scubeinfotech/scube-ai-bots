fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-jose==3.3.0
passlib==1.7.4
PyJWT==2.8.0
python-multipart==0.0.6
requests==2.31.0
aiohttp==3.9.1
beautifulsoup4==4.12.3
python-dotenv==1.0.0
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.0
alembic==1.13.0
apscheduler==3.10.4
pypdf==4.0.0
python-docx==1.1.0

# JavaScript rendering for website crawling
playwright==1.41.0

# Embedding model (bge-m3 by default, configurable via EMBEDDING_MODEL).
# sentence-transformers pulls a CPU build of torch automatically. The model
# weights themselves are NOT in the image — they download to the
# HUGGINGFACE_HUB_CACHE volume on first use.
sentence-transformers==3.0.1

# WhatsApp and messaging integrations
# Optional message brokers (install as needed)
# aio-pika==9.0.5  # For RabbitMQ support
# aiokafka==0.8.1  # For Kafka support
